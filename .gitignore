.envrc
.claude/
.DS_Store
claude/memory/
# nWave-installed skills — generated by `nwave-ai install`, catalogued in
# .nwave-manifest.json; regenerated on every upgrade, so not tracked here.
claude/skills/nw-*/
claude/skills/.nwave-manifest.json
.nwave/
# Debris from test runs with an empty HOME
/.local/
/Library/

# Overlay mount point. A symlink to a private checkout supplying local/links.sh
# and local/zshrc.local; both seams skip cleanly when it is absent.
/local
